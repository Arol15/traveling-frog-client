import { createMuiTheme } from '@material-ui/core/styles'; 

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#c6ff00",
            secondary: 'green'
        }
    }, 
    background: {
        default: '#fff'
    }
})

export default theme; 